{
  "name": "cloud",
  "description": "The hosted build: slim plus the built-in agent runtime and OIDC, so an instance executes agent work with nothing installed on the box and nobody logged into it. Notify already belongs to slim. Every added module lands as Available: see each manifest's autoInstall.",
  "extends": "slim",
  "modules": ["runtime", "oidc"]
}
