{
  "name": "slim",
  "description": "The default build: repositories, agent execution, durable contributor workflows, the daily digest and instance administration. Everything else ships as an installable module in the full build.",
  "modules": ["core", "workspace", "operate", "code", "admin", "plan", "board", "automations"]
}
