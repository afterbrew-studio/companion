{
  "name": "full",
  "description": "Every OSS module in this repo. Slim already carries integrations, notifications, contributor workflows and contribution quality; this adds refinement and ideas, Playground, enterprise sign-in (oidc) and the built-in agent runtime. They land according to each manifest's autoInstall policy.",
  "extends": "slim",
  "modules": [
    "refinement",
    "planner",
    "playground",
    "cursor-bugbot",
    "oidc",
    "runtime"
  ]
}
