{
  "name": "full",
  "description": "Every OSS module in this repo. Slim already carries plan, board and automations for contributor workflows; this adds refinement and ideas, the reactors (slop, playground), and the delivery modules (notify, oidc). They land as Available, not enabled: see each manifest's autoInstall.",
  "extends": "slim",
  "modules": [
    "refinement",
    "planner",
    "slop",
    "playground",
    "notify",
    "oidc"
  ]
}
