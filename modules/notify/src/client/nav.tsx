import { defineNav } from '@moxxy/companion-sdk/client';

/** Delivery history is linked from the unified Integrations pane, so a second
 * settings-nav item would only duplicate that surface. */
export const nav = defineNav([]);
