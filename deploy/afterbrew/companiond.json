{
  "_comment": "Declared providers, adopted when the module activates. Copied into $COMPANION_HOME (/data) at deploy time; this file is the reviewed copy. The credential is taken by indirection through apiKeyEnv so no key is written here, and baseUrl is admin configuration that no prompt, repository file or run argument can influence.",
  "modelProviders": [
    {
      "id": "minimax",
      "label": "MiniMax (subscription)",
      "kind": "openai-compatible",
      "baseUrl": "https://api.minimax.io/v1",
      "apiKeyEnv": "MINIMAX_API_KEY",
      "models": [
        { "id": "MiniMax-M3" },
        { "id": "MiniMax-M2.7" }
      ]
    },
    {
      "id": "zai-coding",
      "label": "Z.AI GLM (coding plan)",
      "kind": "openai-compatible",
      "baseUrl": "https://api.z.ai/api/coding/paas/v4",
      "apiKeyEnv": "ZAI_API_KEY",
      "models": [
        { "id": "glm-4.5" },
        { "id": "glm-4.5-air" }
      ]
    }
  ]
}
